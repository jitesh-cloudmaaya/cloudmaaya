# items to no longer include moving forward and lets run a migration script to exclude/"flag as deleted" existing products
# and set their corresponding category_map allume_category_id = 10 (EXCLUDE) active = 0 pending_review = 0
# the terms are governed as per the spreadsheet and by the model ExclusionTerm (?)