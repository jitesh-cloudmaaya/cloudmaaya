input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-5.1.43-bin.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://mysql:3306/catalogue_service?zeroDateTimeBehavior=convertToNull"
    jdbc_user => "root"
    jdbc_password => "password"
    statement => "SELECT * FROM product_api_products WHERE updated_at > :sql_last_value"
    jdbc_paging_enabled => "true"
    jdbc_page_size => "50000"
    schedule => "* * * * * "
    use_column_value => true
    tracking_column => updated_at
  }
}
output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    user => elastic
    password => changeme
    index => "products"
    document_type => "product"
    document_id => "%{id}"
  }
}
